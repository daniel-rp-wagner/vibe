DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=ratings_api
DB_USER=root
DB_PASS=

# Required for POST .../approve, .../reject and DELETE /api/ratings/{id}
MODERATION_API_KEY=change-me-to-a-long-random-secret

# Optional: comma-separated event types to log (default: all)
# EVENT_LOG_ENABLED=1
